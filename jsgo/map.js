/*const num = [1, 2, 3,]

const square = num.map(sqr);
const cubenum = num.map(cube);

function sqr (element){
    return Math.pow(2, element)
}
function cube (element){
    return Math.pow(3, element)
}

console.log(cubenum);*/

/*const students = ["uday","raj","bobby","ad"];
const studnames = students.map(uppercase);
function uppercase (element){
    return element.toUpperCase(); 
}

console.log(studnames);

const date = ["2020-01-10","2021-01-10","2022-01-10",];
const formtedDate = date.map(datesplit);

function datesplit (element){
    const parts = element.split("-");
    return `${parts[1]}/${parts[2]}/${parts[0]}}`

}

console.log(formtedDate);*/
